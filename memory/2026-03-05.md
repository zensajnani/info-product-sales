# 2026-03-05

## Session Notes

- First session! Zeniqle set up OpenClaw on homelab (`zeneca-homelab-2`)
- Connected via Telegram
- Debugged tool access: file read worked, exec was blocked by `security: allowlist`
- Fixed: patched config to `exec.security = full` + `exec.ask = off`
- Initialized MEMORY.md and memory/ folder
- Fresh install — no prior context
